import React from 'react';

const AddEnrollment: React.FC = () => {
    return (
        <div>
            <h1>Add Enrollment</h1>
            <select>
            
            </select>
        </div>
    );
};

export default AddEnrollment;